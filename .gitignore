# Python
__pycache__/
*.pyc

# 超過 GitHub 單檔 100MB 上限的大檔，預設不入版控。
# 完整資料由 data/derived/ 的 parts 分割檔（每檔 <40MB）保存，
# clone 後可重組正本；若要直接版控大檔，請改用 git LFS 並移除以下兩行。
data/landchg_variation_points_*.csv
raw/*.jsonl

# 錯誤 HTML 快照（本機除錯用，可能很大）
logs/errors/

# 暫存
*.tmp
